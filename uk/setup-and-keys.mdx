---
title: "Налаштування та ключі"
description: "Генеруйте, оновлюйте та відкликайте API ключі pam_mkey для автентифікації Memory MCP."
---

Memory MCP автентифікує кожен запит статичним API ключем агента. Ключі обмежені доступом до retrieval пам'яті для вашого акаунта PAM.

## Формат ключа

```
Authorization: pam_mkey_<prefix>.<secret>
```

Передавайте це значення в заголовку `Authorization` у **кожному** запиті на `/v1/mcp/memory`, включно з handshake-методами (`initialize`, `tools/list`).

<Warning>
  Ніколи не комітьте API ключі в репозиторій. Негайно оновіть ключ, якщо він розкритий.
</Warning>

## Генерація ключа

1. Відкрийте [Memory MCP](https://pam.harmix.ai/for-developers) у застосунку PAM
2. Підключіть джерела пам'яті та дочекайтесь readiness
3. Натисніть **Generate key** і одразу скопіюйте повний секрет

Після виходу зі сторінки секрет більше не відображається. У dashboard видно лише префікс ключа, доки ви його не оновите.

## Один активний ключ на акаунт

На акаунт може бути лише один активний ключ. Під час ротації:

- Видається новий `pam_mkey_<prefix>.<secret>`
- Попередній ключ негайно відкликається
- Потрібно оновити конфігурацію MCP-клієнта

## Ротація або відкликання

| Дія | Де | API |
|-----|-----|-----|
| Оновити ключ | Сторінка Memory MCP | `POST /v1/dev/rotate-key` |
| Відкликати ключ | Сторінка Memory MCP | `POST /v1/dev/revoke-key` |

І дії в dashboard, і REST endpoints потребують **PAM access JWT**, а не `pam_mkey`.

Деталі REST — у [Огляді API для розробників](/uk/developer-api-overview).

## Статус MCP verified

Ключ позначається як **MCP verified** лише після першого **успішного** виклику `retrieve_memory` (`isError: false`).

Заблоковані виклики — quota exceeded, entitlement required, invalid arguments, memory not ready — **не** підтверджують ключ.

## Практики безпеки

- Зберігайте ключі в змінних середовища або secret manager, а не в конфігах репозиторію
- Оновлюйте ключі, коли співробітник залишає команду або ключ міг бути поширений
- Використовуйте окремі ключі для різних середовищ, якщо у вас кілька agent deployments

<Info>
  Секрет показується один раз під час видачі або ротації. Після цього в dashboard Memory MCP видно лише префікс ключа.
</Info>
